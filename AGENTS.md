# AGENTS.md

Full repository agent rules have been moved to:

```text
ActivityAI/rules/AGENTS.md
```

Codex and other AI agents should read that file before non-trivial work. This
root pointer is kept because some agent tools discover `AGENTS.md` only from
the repository root.
